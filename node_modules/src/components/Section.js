import React from 'react';

const Section = ({ title, content }) => {
    return (
        <section>
            <div className="container">
                <h2>{title}</h2>
                <p>{content}</p>
            </div>
        </section>
    );
}

export default Section;
